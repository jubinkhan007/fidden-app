import 'package:flutter/material.dart';

class ConsultationCalendarScreen extends StatelessWidget {
  const ConsultationCalendarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Consultation Calendar')),
      body: const Center(child: Text('Consultation Calendar Coming Soon')),
    );
  }
}
