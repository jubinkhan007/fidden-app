import 'package:flutter/material.dart';

class DesignRequestsScreen extends StatelessWidget {
  const DesignRequestsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Design Requests')),
      body: const Center(child: Text('Design Requests Coming Soon')),
    );
  }
}
